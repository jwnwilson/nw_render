#include "OutputWin.h"

OutputWin::OutputWin(void)
{
}

OutputWin::~OutputWin(void)
{
}
OutputWin::OutputWin(int x,int y)
{
}

void OutputWin::outputImage(const char*c)
{
	/*rdr::window winObj(tempBuff->getWidth(),tempBuff->getHeight());
	for(int y=0;y<tempBuff->getHeight();y++)
	{
		for(int x=0;x<tempBuff->getWidth();x++)
		{
			winObj.set_pixel(x,y,int(tempBuff->getPix(x,y).getRed()*255),int(tempBuff->getPix(x,y).getGreen()*255),
				int(tempBuff->getPix(x,y).getBlue()*255));
		}
	}
	winObj.open();
	winObj.update();
	winObj.wait();
	winObj.close();*/
}